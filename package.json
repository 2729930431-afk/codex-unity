{
  "name": "codex-unity",
  "version": "0.1.0",
  "private": true,
  "description": "Codex-side Unity EditorRpc plugin.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/check-line-counts.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
